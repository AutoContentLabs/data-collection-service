{
  "name": "data-collector",
  "version": "1.0.0",
  "description": "Data Collection Service",
  "main": "src/app.js",
  "scripts": {
    "start": "dotenvx run -f .env.production -- node src/app.js",
    "dev": "dotenvx run -f .env.development -- node src/app.js",
    "test": "dotenvx run -f .env.test -- jest"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@auto-content-labs/fetcher": "github:AutoContentLabs/fetcher",
    "@auto-content-labs/messaging": "github:AutoContentLabs/messaging",
    "@dotenvx/dotenvx": "^1.22.0",
    "winston": "^3.17.0"
  },
  "author": "Quenteon",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}